import deck from './deck';
import players from './players';
import settings from './settings';
import UI from './ui';

export {
  deck,
  players,
  settings,
  UI,
};

const state = {
  deck,
  players,
  settings,
  UI,
};

export default state;
