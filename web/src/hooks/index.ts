import useCardLocation from './useCardLocation';
import useCardsInDeck from './useCardsInDeck';
import useDraw from './useDraw';
import useDropCard from './useDropCard';
import useHand from './useHand';

export {
  useCardsInDeck,
  useCardLocation,
  useDraw,
  useDropCard,
  useHand,
};

const hooks = {
  useCardsInDeck,
  useCardLocation,
  useDraw,
  useDropCard,
  useHand,
};

export default hooks;
